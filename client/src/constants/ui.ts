/**
 * UI Timing, Pagination & Layout Constants
 */

/**
 * Threshold delay (in milliseconds) before rendering skeleton loaders.
 * Prevents jarring visual flicker ("skeleton flash") for rapid or cached network responses.
 */
export const SKELETON_DISPLAY_DELAY_MS = 1000;

/** Default number of items per page in data tables */
export const DEFAULT_PAGE_SIZE = 20;

/** Mobile breakpoint in pixels for responsive triggers */
export const MOBILE_BREAKPOINT = 768;

/** Polling interval (in milliseconds) for RMM telemetry dashboard */
export const RMM_TELEMETRY_POLL_INTERVAL_MS = 30000;
