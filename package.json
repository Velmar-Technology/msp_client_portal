{
  "name": "msp-client-portal",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm -w packages/errors run build && npm -w packages/mcp-server run build",
    "release": "commit-and-tag-version",
    "prepare": "node -e \"if (process.env.NODE_ENV !== 'production') { try { require('child_process').execSync('npx --no husky', { stdio: 'inherit' }); } catch (e) {} }\""
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "commit-and-tag-version": "^12.7.3",
    "husky": "^9.1.7",
    "react": "19.2.6",
    "react-dom": "19.2.6",
    "typescript": "6.0.3"
  },
  "version": "1.8.5"
}
